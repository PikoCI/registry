services:
  postgresql:
    image: postgres:16
    environment:
      POSTGRES_USER: pkreg
      POSTGRES_PASSWORD: pkreg
      POSTGRES_DB: pkreg
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
